# Keep real values in local .env files only.
# Server-side token for the lead delivery bot. Never expose it through VITE_*.
TELEGRAM_BOT_TOKEN=
# Destination chat, group, or channel where the bot can send lead notifications.
TELEGRAM_CHAT_ID=
# Optional Google Sheets archive.
SHEETS_WEBHOOK_URL=
SHEETS_WEBHOOK_SECRET=
